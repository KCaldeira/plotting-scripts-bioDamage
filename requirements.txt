# Core numerics and plotting (main_json.py + plot/)
numpy>=1.22
pandas>=1.5
matplotlib>=3.7
scipy>=1.10
joblib>=1.3

# Geospatial maps (gpp_country_*, number_spatial_statistics)
cartopy>=0.22

# NetCDF / gridded data (utils.func_shared, model data loaders)
xarray>=2023.1.0
netCDF4>=1.6.0

# Regression utilities (utils.func_regression, func_getModelPanada)
statsmodels>=0.14.0

# Regional masks (utils mask helpers; not all are needed for every plot)
regionmask>=0.12.0
geopandas>=0.14.0
shapely>=2.0.0
